Quero criar um projeto Next.js novo do zero com Tailwind CSS v3 e TypeScript.

Contexto: vou usar pra hospedar uma landing page de oferta digital (ticket R$[TICKET]) e talvez área de membros depois. Foco em:
- Carregamento rápido (Core Web Vitals bom)
- Estrutura de pastas escalável
- Identidade visual customizada (cor principal: [COR_HEX])

Me dá:
1. Comando exato pra criar o projeto
2. Arquivos de config que devem ser ajustados (tailwind.config, next.config, tsconfig)
3. Estrutura de pastas recomendada (components, lib, app router)
4. Primeiro componente Hero placeholder
5. Comando pra subir na Vercel

Não me dá explicação acadêmica, só os comandos e arquivos prontos pra copiar/colar.


Quero a estrutura de pastas ideal pra um projeto Next.js que vai ter:
- Landing page de oferta (com variantes A/B)
- Checkout próprio (PIX + cartão)
- Webhook receiver (Kiwify + Mercado Pago)
- Área de membros simples (login com email + senha)
- Painel admin pra ver vendas

Me explica:
1. Estrutura de pastas completa (src/app, src/components, src/lib, etc)
2. Onde colocar tipos compartilhados
3. Onde colocar funções de API
4. Padrão de nomenclatura (camelCase vs kebab-case nas pastas)
5. O que NÃO criar agora (premature abstraction)

Me dá um árvore visual da pasta e um README curto explicando a lógica de cada pasta.

Meu app em produção (Next.js Vercel) começou a dar erro 500 em uma rota.

Logs Vercel:
[COLE_O_LOG_AQUI]

Me ajuda a debugar SEM deploy de teste:
1. O que esse erro provavelmente significa? (3 hipóteses ordenadas por probabilidade)
2. Comandos pra reproduzir localmente?
3. Como chego no estado exato (banco, env, request body) sem afetar prod?
4. Se for env var, como faço diff entre local e produção?
5. Se for problema do banco (Supabase), que query rodar no SQL Editor pra confirmar?

Plano de investigação em ordem (do mais barato pro mais caro de testar).


Preciso criar um endpoint que recebe webhook do Kiwify e atualiza minha base.

Eventos relevantes:
- order_approved, adicionar comprador
- subscription_canceled, marcar inativo
- order_refunded, registrar pra análise
- pix_generated (PIX gerado mas não pago), fila de recuperação

Requisitos críticos:
1. Validar assinatura HMAC do header (KIWIFY_SECRET no env)
2. Idempotência (mesmo evento pode chegar 2x), event_id como chave única
3. Responder 200 RÁPIDO (em <2s) e processar em background
4. Logar TUDO num jsonb (tabela webhook_logs)
5. Falha interna enfileira retry, NÃO devolve 500

Stack: Next.js (App Router) + Supabase.

Me dá:
1. Schema das tabelas
2. POST /api/webhook/kiwify completo
3. validarAssinaturaKiwify(body, signature)
4. Lógica de retry/queue
5. Exemplo de payload de cada evento (curl)

Quero uma função utilitária TypeScript reutilizável que valida webhooks de várias plataformas.

Requisitos:
1. HMAC-SHA256 (Kiwify, Stripe)
2. HMAC-SHA1 (algumas antigas)
3. Validar timestamp (recusa requests >5 min de idade, previne replay attack)
4. Type-safe: { algorithm, secret, signatureHeader, timestampHeader? }
5. Retornar { valid: boolean, motivo?: string }

Me dá:
1. lib/webhook-validator.ts completo
2. Exemplos pra Kiwify, Stripe, Mercado Pago
3. Testes unitários básicos com Vitest (4 casos)

Código de produção.



Preciso criar uma API PIX com Mercado Pago no meu Next.js (App Router).

Fluxo:
1. POST /api/pagamento/criar, recebe { nome, email, valor } e gera cobrança PIX. Devolve { id_pagamento, qr_code_base64, qr_code_copia_cola, expira_em }
2. GET /api/pagamento/status/[id], polling do frontend pra saber se já foi pago. Devolve { status: 'pendente' | 'pago' | 'expirado' }
3. POST /api/pagamento/webhook, recebe notificação do Mercado Pago quando muda status. Atualiza banco.

Contexto:
- Token do MP em env var (MP_ACCESS_TOKEN)
- Banco: Supabase Postgres
- Tabela: pagamentos (id, mp_id, email, nome, valor, status, criado_em, pago_em)

Me dá:
1. Schema SQL da tabela
2. Código completo dos 3 endpoints
3. Função utilitária pra chamar API do MP (lib/mercadopago.ts)
4. Validação de assinatura do webhook
5. Tratamento de erro com retry exponencial

Código pronto pra colar.


Quero construir um checkout próprio pra minha oferta de R$[TICKET], substituindo Kiwify pra economizar taxa.

Requisitos:
- Página /checkout com formulário (nome, email, CPF, telefone)
- 2 métodos: PIX (gera QR) e cartão (Mercado Pago tokenization)
- Após aprovado, redireciona pra /obrigado e dispara evento Purchase
- Order bump: 1 produto adicional pré-marcado
- 1-click: se email já existe com cartão salvo, opção "comprar com *1234"
- Reembolso 7 dias por padrão

Me dá:
1. Estrutura de tabelas (compradores, cartoes_salvos, pedidos, order_bumps)
2. Componente <Checkout /> completo
3. Componente <OrderBump /> reutilizável
4. Lógica de 1-click
5. Endpoints de processamento
6. Como integrar pixel Meta no momento certo

Quero código completo, sem placeholder.


Quero criar uma landing page de alta conversão pro meu produto:

- Nome: [NOME_PRODUTO]
- Promessa: [PROMESSA]
- Persona: [PERSONA]
- Ticket: R$[TICKET]
- Mecanismo único: [MECANISMO]
- Garantia: 7 dias

Estrutura (em ordem):
1. Hero, headline + subheadline + CTA + VSL placeholder
2. Prova social, 3-4 logos/menções
3. Quem é (você), bio curta com prova
4. Pra quem É e pra quem NÃO É, duas listas
5. Mecanismo único, bento grid 4-6 cards
6. O que tá incluso, bullets
7. Oferta, preço com âncora, garantia, CTA grande
8. FAQ, accordion 6-8 perguntas
9. Footer

Stack: Next.js App Router + Tailwind v3.

Identidade: cor [COR], fundo dark, fonte Space Grotesk.

Me dá:
1. /page.tsx completa
2. Cada componente separado
3. Animações sutis (fade-up no scroll)
4. Responsivo de verdade

Usa textos placeholder que façam sentido pro produto.


Quero um componente Bento Grid em React + Tailwind que renderiza 4-6 cards de tamanhos diferentes.

Requisitos:
- Layout responsivo: 1 col mobile, 2-3 col desktop
- Cards de tamanhos variados (2x1, 1x1, 2x2 destaque)
- Cada card: { titulo, descricao, icone (lucide-react), tamanho, accent? }
- Card com `accent` tem borda/glow na cor primária
- Hover sutil (lift + glow aumenta)
- Background glass

Identidade: cor [COR], fundo dark.

Me dá:
1. <BentoGrid items={...} /> e <BentoCard />
2. 6 itens de exemplo preenchidos
3. CSS/Tailwind classes prontas
4. Sem dep externa além de lucide-react